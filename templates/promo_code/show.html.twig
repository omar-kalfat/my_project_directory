{% extends 'admin-base.html.twig' %}

{% block title %}PromoCode{% endblock %}

{% block body %}
    <h1>PromoCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo_code.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ promo_code.code }}</td>
            </tr>
            <tr>
                <th>Reduction</th>
                <td>{{ promo_code.reduction }}</td>
            </tr>
            <tr>
                <th>Expirydate</th>
                <td>{{ promo_code.expirydate ? promo_code.expirydate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promo_code_index') }}">back to list</a>

    <a href="{{ path('app_promo_code_edit', {'id': promo_code.id}) }}">edit</a>

    {{ include('promo_code/_delete_form.html.twig') }}
{% endblock %}
